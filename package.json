{
  "name": "imdb-bloom-filter",
  "version": "1.0.0",
  "description": "Bloom filter for IMDB ID validation",
  "type": "module",
  "main": "dist/cli.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "cli-progress": "^3.12.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "typescript": "^5.8.3",
    "vitest": "^4.1.6"
  }
}
